using UnityEngine;

public class OceanFog : MonoBehaviour
{
    private Material fog;
    private Color emissionColor;

    private void Awake()
    {
        //emissionColor = fog.GetColor("_EmissionColor");
    }
    private void Update()
    {
        //fog.SetColor(emissionColor, sunligh)
    }
}
